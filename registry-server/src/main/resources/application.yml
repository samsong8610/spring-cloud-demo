eureka:
  instance:
    hostname: localhost
  client: # Not a service, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 1111

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG